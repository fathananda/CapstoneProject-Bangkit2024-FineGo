<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyFirebaseAuth" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:actionOverflowMenuStyle">@style/OverflowMenu</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/firebase_amber</item>
        <item name="colorPrimaryVariant">@color/firebase_orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/firebase_blue</item>
        <item name="colorSecondaryVariant">@color/firebase_navy</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="OverflowMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:icon">@drawable/ic_profile</item> <!-- Warna ikon -->
    </style>

</resources>